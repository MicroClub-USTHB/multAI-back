"""
This type stub file was generated by pyright.
"""

from enum import Enum
from typing import Dict, Iterable, Optional, Tuple, Union
from .credentials import Credentials
from .payload import Payload

class NotificationPriority(Enum):
    Immediate = ...
    Delayed = ...


class NotificationType(Enum):
    Alert = ...
    Background = ...
    VoIP = ...
    Complication = ...
    FileProvider = ...
    MDM = ...


RequestStream = ...
Notification = ...
DEFAULT_APNS_PRIORITY = ...
CONCURRENT_STREAMS_SAFETY_MAXIMUM = ...
MAX_CONNECTION_RETRIES = ...
logger = ...
class APNsClient:
    SANDBOX_SERVER = ...
    LIVE_SERVER = ...
    DEFAULT_PORT = ...
    ALTERNATIVE_PORT = ...
    def __init__(self, credentials: Union[Credentials, str], use_sandbox: bool = ..., use_alternative_port: bool = ..., proto: Optional[str] = ..., json_encoder: Optional[type] = ..., password: Optional[str] = ..., proxy_host: Optional[str] = ..., proxy_port: Optional[int] = ..., heartbeat_period: Optional[float] = ...) -> None:
        ...
    
    def send_notification(self, token_hex: str, notification: Payload, topic: Optional[str] = ..., priority: NotificationPriority = ..., expiration: Optional[int] = ..., collapse_id: Optional[str] = ...) -> None:
        ...
    
    def send_notification_async(self, token_hex: str, notification: Payload, topic: Optional[str] = ..., priority: NotificationPriority = ..., expiration: Optional[int] = ..., collapse_id: Optional[str] = ..., push_type: Optional[NotificationType] = ...) -> int:
        ...
    
    def get_notification_result(self, stream_id: int) -> Union[str, Tuple[str, str]]:
        """
        Get result for specified stream
        The function returns: 'Success' or 'failure reason' or ('Unregistered', timestamp)
        """
        ...
    
    def send_notification_batch(self, notifications: Iterable[Notification], topic: Optional[str] = ..., priority: NotificationPriority = ..., expiration: Optional[int] = ..., collapse_id: Optional[str] = ..., push_type: Optional[NotificationType] = ...) -> Dict[str, Union[str, Tuple[str, str]]]:
        """
        Send a notification to a list of tokens in batch. Instead of sending a synchronous request
        for each token, send multiple requests concurrently. This is done on the same connection,
        using HTTP/2 streams (one request per stream).

        APNs allows many streams simultaneously, but the number of streams can vary depending on
        server load. This method reads the SETTINGS frame sent by the server to figure out the
        maximum number of concurrent streams. Typically, APNs reports a maximum of 500.

        The function returns a dictionary mapping each token to its result. The result is "Success"
        if the token was sent successfully, or the string returned by APNs in the 'reason' field of
        the response, if the token generated an error.
        """
        ...
    
    def update_max_concurrent_streams(self) -> None:
        ...
    
    def connect(self) -> None:
        """
        Establish a connection to APNs. If already connected, the function does nothing. If the
        connection fails, the function retries up to MAX_CONNECTION_RETRIES times.
        """
        ...
    


